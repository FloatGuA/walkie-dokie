# Decision Log

> 决策来源：与用户在 claude.ai 上的规划对话（[存档](https://claude.ai/share/14e91185-54c3-4b48-b860-dc11d6dfc690)），2026-08-07 迁移为正式项目并补记于此。

## 项目命名定为 walkie-dokie

- **日期**：2026-08-07
- **背景**：需要一个同时服务两类观众的项目名——招聘方/GitHub（英文、好记、有画面感）和真实用户（阿姨、师傅：中文、两三个字、能喊出口）。
- **选了什么**：repo 名 `walkie-dokie`，英文全称 *Walkie-Dokie — Multi-Agent Work Assistant for Frontline Service Workers*，中文名「小帮」。walkie（对讲机 / 语音优先）+ dokie（okie-dokie 的应答语，对应产品核心交互"说一句话，系统回一句"）构成双关，直接映射产品的语音优先、低认知负担、短回合设计。已核实 GitHub / npm / PyPI 均无同名占用。
- **否掉了什么，为什么**：
  - `Toolbelt`／「顺手」——偏工程师视角，一线用户听不懂，且 toolbelt 在开源生态里被用滥。
  - `Rounds`／「巡楼」——英文太抽象，不看说明猜不到含义。
  - `Doorbell`／「门铃」——画面感强但听起来像消费级产品，弱化了多 Agent 系统的技术分量。
- **代价 / 已知不足**：`Walkie-Dokie` 单独出现容易显得像 hackathon 玩具项目，不带"可靠"的联想。
- **什么情况下应该重新考虑**：如果简历/README 中始终无法保证名字与英文副标题（*Multi-Agent Work Assistant for Frontline Service Workers*）成对出现，考虑换回更"正经"的名字。

## 主编排框架选 LangGraph

- **日期**：2026-08-07
- **背景**：报修工单是有生命周期的状态机（待确认 → 待派单 → 执行中 → 待验收），且流程中需要暂停等待人工审批或用户回复。
- **选了什么**：LangGraph 作主编排层，用其 checkpoint + interrupt 机制承载状态机与人工介入（HITL）。
- **否掉了什么，为什么**：未展开对比其他编排框架（如纯手写状态机、CrewAI 等）——LangGraph 的 checkpoint/interrupt 语义与"暂停等人类"这一核心需求直接对应，未见到不选它的理由。
- **代价 / 已知不足**：暂无实测，状态机的实际复杂度和 LangGraph 的适配度要等第一条闭环跑通才能验证。
- **什么情况下应该重新考虑**：如果实现中发现 LangGraph 的 checkpoint 机制无法自然表达"多轮澄清 + 二次确认"的暂停/恢复需求。

## 模型 SDK 按任务类型分层：OpenAI Agents SDK vs Anthropic SDK

- **日期**：2026-08-07
- **背景**：项目同时是求职作品，需要能讲清楚"为什么用两个 SDK"而不是"因为都流行"。
- **选了什么**：OpenAI Agents SDK 承担高频、低成本、确定性强的短任务（意图分类、槽位抽取）；Anthropic SDK 承担需要判断力的任务（澄清策略选择、危险操作风险审核、对外沟通措辞的分寸）。
- **否掉了什么，为什么**：单一 SDK 走到底的方案——面试/工程上都需要能讲清楚成本与能力分层的依据，用一个 SDK 无法体现这层设计。
- **代价 / 已知不足**：引入两套 SDK 的维护和调用成本，且分层边界（什么算"需要判断力"）目前是经验判断，未经实测校准。
- **什么情况下应该重新考虑**：如果 eval 数据显示某类任务用低成本模型也能达标，或反之高频任务的错误率过高需要升级模型，分层边界需要重新调整。

## 跨组织协作用 A2A，而非内部函数调用包一层 HTTP

- **日期**：2026-08-07
- **背景**：物业公司和家政平台是两个独立组织、两套系统，业主报修后可能需要从物业 Agent 委派给家政平台 Agent（如"这是深度保洁不是维修"）。
- **选了什么**：用 A2A 协议实现跨组织的 Agent 委派，自建两个服务、两张 Agent Card 做演示。
- **否掉了什么，为什么**：用内部函数调用包一层 HTTP 假装分布式——这不是 A2A 被设计出来要解决的问题（跨供应商 Agent 互操作），会在面试中被识破是伪造的演示。
- **代价 / 已知不足**：需要额外搭建第二个服务和 Agent Card，增加实现量；路线图上排在后期（第 5-6 周），做不完不影响主线。
- **什么情况下应该重新考虑**：如果时间不够，A2A 部分可以整体砍掉而不影响单条闭环的完整性——这在范围收敛决策中已预先说明。

## n8n 只做确定性集成调度，不做 Agent 编排

- **日期**：2026-08-07
- **背景**：n8n 和 LangGraph 都有"编排"能力，容易混用导致职责不清。
- **选了什么**：n8n 只承担确定性部分——企业微信 webhook 接入、定时推送排班、工单超时告警、结算 Excel 导出、短信对接。
- **否掉了什么，为什么**：用 n8n 做 Agent 编排——面试会被问穿，且不确定性的理解与决策应该由 LangGraph 承担，n8n 负责确定的集成与调度，两者职责必须分开说清楚。
- **代价 / 已知不足**：无，这是职责边界的划分，不涉及功能取舍。
- **什么情况下应该重新考虑**：不预期会重新考虑，除非未来发现某类调度逻辑既非纯确定性也非需要 LLM 判断，处于灰色地带。

## 危险操作分三档处理

- **日期**：2026-08-07
- **背景**：Agent 的输出有真实副作用（会派单、会给业主发消息），需要明确的风险控制策略。
- **选了什么**：三档——查询类直接执行；对外发消息 / 派单 / 改工单状态需用户二次确认；退款、投诉升级、删除记录必须转人工主管，Agent 无自动执行权限。误触发率指标：危险操作在未经确认时被执行的次数必须为 0。
- **否掉了什么，为什么**：更简单的两档方案（安全 / 需确认）——被否是因为退款、投诉升级、删除记录这类操作的后果超出"二次确认"能覆盖的风险等级，需要完全排除 Agent 的自动执行权限，而不只是加一道确认。
- **代价 / 已知不足**：三档的判定逻辑（哪些操作落在哪一档）需要在实现中明确列出规则，目前只有分类原则，没有完整清单。
- **什么情况下应该重新考虑**：如果 eval 发现某类操作的分档判断本身经常出错，需要重新审视分档标准或增加人工复核层。

## 范围收敛：先做深单条闭环，A2A/n8n 作为后期加分项

- **日期**：2026-08-07
- **背景**：技术栈清单（LangGraph + 两个 SDK + A2A + n8n + MCP + RAG + eval）全部铺开，面试官追问时容易露怯——"会连接组件"不等于"会让 Agent 稳定不出错"。
- **选了什么**：先把"语音报修 → 澄清 → 派单 → 确认"这一条链路做到有 eval 数据、有 trace、有失败案例分析，再往外加。路线图：第 1-2 周单闭环打通；第 3-4 周澄清 Agent + 记忆 + HITL + RAG；第 5-6 周 A2A 拆分 + n8n + trace；第 7 周 eval + README + demo。
- **否掉了什么，为什么**：技术栈全铺开、每样都做的方案——用户认可"宁可只有 3 个 Agent 但每个都有指标，也不要 8 个 Agent 全是 demo"这一判断。
- **代价 / 已知不足**：A2A 和 n8n 存在做不完的风险，被明确标注为"做不完不影响主线"。
- **什么情况下应该重新考虑**：如果单条闭环提前完成且质量达标（有完整 eval 指标），可以提前启动 A2A/n8n 部分。

---

> 以下决策发生于 2026-08-07 同一天的后续讨论，**推翻了上面除「项目命名」外的全部方向性决策**。旧条目保留，不删除——被否的理由本身是信息。

## 项目方向由「物业家政多 Agent 平台」转为「面向中老年人群的多平台办公助手」

- **日期**：2026-08-07（同日后续讨论）
- **背景**：用户重新审视后发现，物业/家政场景与真正想做的东西"大相径庭"。真实诉求是：中老年用户不太会操作电脑，但有 Word 文档撰写、Excel 表格处理的需求；接入方式类似"openclaw"——泛指同时接微信/企业微信/QQ 的机器人网关。这是**用户拍板的产品方向变更**，不是技术推导。
- **选了什么**：同一个 repo（`walkie-dokie`）重新定位为「多平台机器人办公助手」，核心场景是 Word/Excel 的生成、编辑、问答。旧的物业家政相关代码（`WorkOrderState`、澄清 Agent、A2A 跨组织委派、n8n 工单调度等目录结构）全部清除。
- **否掉了什么，为什么**：物业家政多 Agent 平台方向——用户明确表示"可以等有空再弄"，不是当前优先级。这直接推翻/搁置了上面「跨组织协作用 A2A」「n8n 只做确定性集成调度」「危险操作分三档处理」「范围收敛：先做深单条闭环」四条决策——它们描述的具体机制（工单风险分级、跨组织委派、周更路线图）绑定在物业家政场景上，场景不在了，机制暂时也用不上。「模型 SDK 按任务类型分层」一条被下面的新决策取代（见"文档操作用 coding agent 当执行后端"）。
- **代价 / 已知不足**：之前的详细设计（澄清策略、风险分级三档、eval 数据集设计思路）作废。但相关设计方法论——"确认用回 1/回 2"、三层记忆、HITL 暂停等待——在新方向的适老化设计上仍可能复用，需要时再评估，不算完全浪费。
- **什么情况下应该重新考虑**：如果未来精力允许，物业家政方向可以作为独立 project 重新捡起，届时上面被搁置的四条决策可以直接复用。

## 文档操作不自建 DSL，用 coding agent（Claude Agent SDK / Codex）当执行后端

- **日期**：2026-08-07
- **背景**：核心场景包含"从零生成""编辑修改""读取问答"三类文档操作需求。若自建一套文档操作的业务逻辑/DSL，工作量大且容易做浅——这也是求职作品最容易露怯的地方。
- **选了什么**：执行层直接复用具备代码执行能力的 coding agent（Claude Agent SDK / Codex），让它在沙箱里写代码（python-docx / openpyxl 之类）操作 docx/xlsx。**用户明确要求两个后端都接、可切换**，不锁定单一供应商。**取代**上面「模型 SDK 按任务类型分层：OpenAI Agents SDK vs Anthropic SDK」一条——不再需要按任务类型手工分层调用两个 SDK 做意图分类/槽位抽取，理解和执行都交给 coding agent 自己的 agentic loop。
- **否掉了什么，为什么**：自建文档操作的业务 DSL / 规则引擎——用户明确说"不一定要从头开始写 harness"，两个现成 coding agent 都能通过写代码完成文档操作，没必要重复造轮子。
- **代价 / 已知不足**：依赖执行后端自带的代码执行沙箱是否稳定、安全边界如何划定，目前尚未设计，需要在实现前补上（用户上传的文件 + agent 生成的代码同时进沙箱，是一个明确的安全面）。
- **什么情况下应该重新考虑**：如果两个执行后端在实测中因文档格式复杂度、沙箱限制等原因效果不稳定，可能需要为特定高频操作（如简单格式转换）单独写专用工具，而非完全依赖 coding agent 现场写代码。

## 保留 LangGraph，但职责从「管工单生命周期」收窄为「管跨消息的会话状态机」

- **日期**：2026-08-07
- **背景**：执行 agent（Claude Agent SDK/Codex）本身自带 agentic loop，不需要 LangGraph 管它们内部怎么写代码；但用户的多轮对话（"文件收到但指令不明确""执行中""等确认""完成"）仍然需要跨消息的状态持久化和暂停/恢复能力——这是询问 Claude 后由 Claude 给出建议、用户确认采纳的决策。
- **选了什么**：LangGraph 只负责会话层状态图（`SessionState`：platform / user_id / pending_file / instruction / backend / status / result_file），把"调用执行 agent"当作图里的一个黑盒节点。执行 agent 内部的代码撰写、调用、重试完全不归 LangGraph 管。**修订**上面「主编排框架选 LangGraph」一条——保留框架选型，但状态机的建模对象从"工单生命周期"改为"会话生命周期"。
- **否掉了什么，为什么**：完全去掉 LangGraph、只用轻量状态存储（如按用户 ID 存最近上下文）——被否是因为这等于自己重新发明一个简化版 LangGraph，而 LangGraph 的 checkpoint/interrupt 机制正好匹配"可插拔执行后端 + 跨消息暂停等待"的需求，且它对执行后端是谁无感知，天然契合可切换的要求。
- **代价 / 已知不足**：`SessionState` 的实际字段和图的节点/边定义还没有实现验证。
- **什么情况下应该重新考虑**：如果实现中发现会话状态机的复杂度远低于预期（比如实际上只有"等指令→执行→完成"三态，没有真正需要 interrupt 的暂停场景），可以考虑降级为更轻量的状态管理。

## MVP 平台接入：先建可插拔适配层，第一个具体实现选企业微信自建应用

- **日期**：2026-08-07
- **背景**：微信个人号（wechaty/itchat 类）有封号风险，不适合长期稳定运行；QQ 协议方案（go-cqhttp/napcat 类）技术成熟但目标用户中老年群体实际使用率在下降；企业微信自建应用是官方 API、稳定合规，但对"帮父母长辈"这类纯个人场景略显门槛高。
- **选了什么**：先抽象出统一的 Event 接口（`PlatformAdapter`），第一版具体实现企业微信适配器跑通，其余平台（QQ、微信）后续按同一接口补齐。**用户拍板**：MVP 平台选企业微信 + 坚持要可插拔接口，两者都要。
- **否掉了什么，为什么**：直接锁定微信个人号方案——封号风险不适合作为长期稳定运行的项目基础；直接锁定 QQ 方案——目标用户群体使用率是下降趋势，优先级低于企业微信。
- **代价 / 已知不足**：企业微信自建应用要求有企业/组织身份注册，对纯"帮个人长辈"的场景有一定门槛，后续如果要覆盖真实中老年个人用户，可能仍需要补上微信个人号或 QQ 适配器。
- **什么情况下应该重新考虑**：如果实测发现目标用户群体几乎不用企业微信，需要提前把微信个人号或 QQ 适配器的优先级提上来。

## MVP 阶段接受 ToS 边界不确定性，先用 Claude 订阅登录测试

- **日期**：2026-08-07
- **背景**：[PITFALLS.md](PITFALLS.md) 记录了 Anthropic 官方文档的措辞——Agent SDK 用于"你提供给客户/终端用户的产品或服务"时不允许用订阅（claude.ai）登录，必须用 API key。但这句话是否覆盖"目前只有开发者本人在用、还没有其他终端用户"这种场景，措辞本身有解释空间，Claude（我）没有把握确认单人自用一定不触发——这条限制读起来更像按"架构形态是不是个服务"界定，而不是按"当前用户数量"界定。
- **选了什么**：**用户拍板**——"我纯粹给自己用的"，MVP 阶段先用 Claude 订阅登录（`claude login`）测试 Agent SDK 执行后端，不申请 API key。
- **否掉了什么，为什么**：立刻切 API key——用户认为当前只有自己一个人用，风险可接受，优先图方便和省钱；Claude 本身对这条 ToS 边界持保留态度，已明确告知用户这不是"确认合规"，是用户自己承担的判断。
- **代价 / 已知不足**：ToS 边界本身不确定，订阅登录用于常驻 webhook 服务存在被限流或被判定超出许可范围的风险，且这个风险没有被验证/排除，只是被知情接受。
- **什么情况下应该重新考虑**：一旦有除开发者本人之外的真实用户（哪怕只是家人）开始通过这个 bot 使用，**必须**切换到 API key，不能继续用订阅登录——这是硬性红线，不是"以后再说"。

## 平台改用个人微信（wxauto），推翻此前"否掉微信个人号"的决策

- **日期**：2026-08-08
- **背景**：企业微信自建应用因缺少企业资质暂时用不了；调研发现企业微信「智能机器人」和微信公众号客服消息接口都**不支持发送通用文件**（docx/xlsx），只能发文本/图片/语音/视频/卡片类消息；QQ 官方机器人平台个人开发者同样无法注册，卡在企业资质上。而"能把生成的文件发给用户"是这个项目不能退让的核心能力。个人微信作为完整的 IM 客户端，原生支持任意文件发送，且是目标用户（中老年人）最熟悉、每天在用的产品——这几点是"合规平台"都给不了的。
- **选了什么**：**用户拍板**——"先用自己的微信当 bot 吧，有封号风险，但是没关系"，明知有风险仍选择接受。技术上选 `wxauto`（基于 UIAutomation 操控 Windows 版微信客户端，不是内存注入/hook，相对更低检测风险），因为开发机就是 Windows，且它直接支持 `SendFiles()`。
- **否掉了什么，为什么**：继续在"合规平台"里找发文件的方案（QQ 官方、Telegram 等）——QQ 官方同样卡资质，Telegram 目标用户覆盖率太低，都不解决当下问题；发预览链接代替真实文件的折中方案——个人微信原生支持发文件后，不再需要这个折中，可以留作其他平台以后的备选。这条推翻了此前 [MVP 平台接入] 决策里"否掉微信个人号——封号风险不适合长期稳定运行"的判断——彼时是在"企业微信可用"的前提下做的权衡，前提变了（企业微信实际不可用），风险接受意愿也变了（用户明确表示可以接受）。
- **代价 / 已知不足**：封号风险真实存在且没有消除，只是用户知情接受；`wxauto` 依赖特定微信客户端版本（wxauto4 目前只支持微信 4.0.5），微信客户端自动更新可能随时打破兼容性；运行模式也从"无状态 webhook 服务"变成"本机常驻进程 + 桌面版微信保持登录"，部署形态和之前设计的架构不一样，`platforms/` 下的适配器实现需要按这个模型重新设计（监听循环而不是 HTTP 回调）。
- **什么情况下应该重新考虑**：如果账号真的被封，或者微信客户端更新导致 `wxauto` 大范围失效且没有及时的替代方案；或者企业微信资质问题后续解决了，可以把企业微信作为更稳定的备选路径重新拾起。

## MVP 平台再改为飞书自建应用，推翻上一条"改用个人微信 wxauto"的决策

- **日期**：2026-08-08（同日内，wxauto 决策定下后不久）
- **背景**：拍板 wxauto 之后，用户主动问"飞书要不要企业资质"，一查发现飞书自建应用不需要企业资质即可创建（个人可注册），且官方 Python SDK `lark-oapi` 提供长连接收消息（不需要公网回调地址/Token/EncryptKey）和 REST API 发文件（`file_key` 机制），文件发送能力和 wxauto 一样齐全，但没有 wxauto 的封号风险、也没有微信客户端版本兼容性这个隐患。实测验证：飞书 App ID/Secret 有效、鉴权成功、长连接跑通、真实发消息生成文档全链路成功。
- **选了什么**：MVP 技术主线改为飞书自建应用（长连接模式）。这条**不是否定**"个人微信更贴近真实中老年用户"这个判断——那个判断依然成立，只是被拆成了两个独立目标：飞书当技术验证/求职作品的主线（工程上最省心、最快出效果），个人微信（真给家人用）作为后续单独任务，不在这次 MVP 范围内。用户原话确认了这个理解："先在飞书上把端到端跑通，个人微信作为后续单独任务"。
- **否掉了什么，为什么**：继续按 wxauto 方案往下做——wxauto 依赖特定微信客户端版本号（当时装的微信是 4.1.12.26，wxauto4 官方只写明支持 4.0.5 这一个具体版本），用户自己提出"微信版本迭代很快"这个顾虑，而且封号风险始终是真实存在、未消除的代价；飞书用官方 SDK、零封号风险、不挑客户端版本，工程稳定性明显更高。
- **代价 / 已知不足**：飞书不是目标用户（中老年人）会自己用的产品，这个矛盾没有被解决，只是被"技术主线 vs 真实交付"这个拆分暂时搁置了——早晚要单独做个人微信（或其他中老年人熟悉的平台）这条线，飞书本身不能算最终交付形态。
- **什么情况下应该重新考虑**：飞书主线的技术验证/求职作品目标达成之后，需要重新拾起"给真实中老年用户用"这条线时，参考 `platforms/feishu.py` 的架构模式（尤其是长连接桥接到 `asyncio.Queue` 那一套，见 TECHNICAL.md）去接 wxauto 或其他平台。

## 执行 agent 的工作目录从"用完即焚的临时目录"改为落在项目里、不自动清理

- **日期**：2026-08-08
- **背景**：早期实现用 `tempfile.TemporaryDirectory()`，执行完就连着生成的文件一起被系统清空，出问题时（比如 Claude 汇报的文件名和实际保存的文件名对不上那次）没法事后打开目录看到底发生了什么，只能靠日志文字描述硬猜。用户明确提出"以后要把文件放在合适的地方"，并且说"项目初期存的细一点，没关系"。
- **选了什么**：新增 `src/walkie_dokie/workspace.py` 的 `create_workspace_dir(platform, user_id)`，工作目录固定放在 `var/workspaces/{platform}_{user_id}/{日期}/{run_id}/`，不自动删除。`ExecutionAgent.run()` 接口加了 `workdir: Path` 参数，工作目录由调用方（orchestrator）创建并传入，执行后端不再自己决定目录放哪。
- **否掉了什么，为什么**：继续用 `tempfile.TemporaryDirectory()`——这是 Python 处理临时文件的常规最佳实践（自动清理、不占地方），但和"项目早期要能复盘"这个明确需求冲突，用户拍板要留痕优先于整洁。
- **代价 / 已知不足**：磁盘占用会一直涨，没有任何自动清理/归档策略，长期跑下去 `var/workspaces/` 会变得很大；用户生成的文档内容会长期留在本地磁盘上，如果以后真的给其他人（比如家人）用，这里涉及别人的文件内容留存，需要重新评估隐私边界（现在只有开发者一人用，暂不算问题）。
- **什么情况下应该重新考虑**：项目过了高频调试阶段、或者开始有除开发者之外的真实用户时，需要补上清理策略（比如按 N 天过期自动删除，或者只在出错时才保留现场）。

## orchestrator 加回一道确认环节——防抖攒消息 + 生成任务提示词草稿 + 等用户确认才执行

- **日期**：2026-08-08
- **背景**：实测发现两个问题——① 执行 agent（Claude Agent SDK）对短/模糊指令的意图理解"很一般"，直接执行容易文不对题；② 同一用户连续发的消息（比如先说"帮我写个合同"再补一句细节）会被当成两个独立请求并发处理，而不是同一个意图的补充。此前在 [保留 LangGraph...] 决策附近，我们明确决定过"不做 `awaiting_confirm` 这类确认状态"，理由是文档生成没有真实副作用风险，跟物业家政场景"确认怕误操作"的性质不同。
- **选了什么**：**用户拍板**——引入 10 秒防抖窗口（同一用户新消息进来就重置计时器，10 秒无新消息才算一轮攒够了）；攒够后用一次轻量 LLM 调用（不给工具、不跑代码）把用户的话提炼成一句清楚的 task prompt 草稿；把这句草稿回复给用户确认；用户确认了才把这个 task prompt 正式喂给执行 agent，用户如果说别的/补充，就并入这轮消息重新生成草稿再问一次。"等确认"这一步用 LangGraph 的 checkpoint/interrupt 机制做暂停。
- **否掉了什么，为什么**：把"生成草稿"这一步做成一个正式的 tool——用户明确说不需要，就是一次轻量调用，不用工具化。**这条不推翻**"不做 awaiting_confirm"那条旧决策——旧决策否掉确认环节的理由是"没有真实副作用风险"，这条理由依然成立；这次加确认环节的理由是另一个不同的动机："意图理解准头不够，用便宜的确认环节提前纠错，比让执行 agent 直接跑一次几十秒的完整生成再发现理解错了要划算"，是成本/准确率的权衡，不是风险控制。
- **代价 / 已知不足**：每一轮多了至少一次额外的轻量 LLM 调用和一次用户往返，交互链路变长；10 秒防抖窗口的具体时长是拍脑袋定的，没有实测调过。
  - **实现状态更新（2026-08-17）**：`orchestrator/debounce.py` 已实现并接入 `run_mvp.py`。文字聚合按设计工作（窗口内多条文字拼成一段）；但文件只有单槽覆盖——`self._files[key] = file` 同一 key 下新文件直接覆盖旧文件，不是队列。窗口内连续发多个文件时，只有最后一个会被喂给 agent，前面的被静默丢弃，且没有告知用户。这是"架构设计支持文字+文件聚合、实现目前只做到单文件"之间的真实缺口，不是这条决策本身的问题——决策要解决的是"意图理解准头不够"和"消息被拆成几轮"，没有专门就多文件场景展开设计。
  - **后续（2026-08-18）**：这条缺口的设计已定稿，见下方「多文件执行会话设计」决策与对应 spec；代码尚未实现。
- **什么情况下应该重新考虑**：如果实测发现 10 秒窗口经常等太久（用户等得不耐烦）或者太短（消息经常被拆成好几轮），需要调整或者换成更智能的触发方式（比如判断消息是否像"说完了"而不是纯定时）。

## 两个执行后端都要跟开发者本机的个人 AI 配置彻底隔离

- **日期**：2026-08-09
- **背景**：实测 `CodexBackend` 时，回复里混进一个跟任务无关的前缀"FGuA"，排查发现是开发者本机 `~/.codex/AGENTS.md` 里的个人全局约束泄漏进了执行结果——`codex exec`（本地 CLI）不管走订阅还是 API key 鉴权，都会读同一份 `$CODEX_HOME`（默认 `~/.codex`）下的 config.toml/AGENTS.md/rules/skills，这些是开发者过去几个月在别的项目里攒下来的个人定制，跟 walkie-dokie 完全无关。类似地，`ClaudeAgentSDKBackend` 默认也会加载开发者本机 `~/.claude` 的全局设置。
- **选了什么**：`ClaudeAgentSDKBackend`/draft 生成两处调用都传 `ClaudeAgentOptions(setting_sources=[])`（SDK 自带的"隔离模式"，一个参数关掉所有文件系统配置加载）；`CodexBackend` 改为使用独立的 `CODEX_HOME`（`var/codex_home/`，不进 git），跟开发者本机的 `~/.codex` 完全分开，需要单独 `codex login` 一次。
- **否掉了什么，为什么**：一开始想给 `codex exec` 加 `--ignore-user-config`/`--ignore-rules` 这类单项跳过 flag——排查过程中发现开发者本机的污染源不止一个（config.toml、AGENTS.md、rules 三层各自独立），一个个加 flag 属于打地鼠，以后开发者本机再新增别的个人定制还会重新泄漏；换独立 `CODEX_HOME` 是一次性彻底隔离，以后开发者本机配置怎么变都不影响这个项目。
- **代价 / 已知不足**：独立 `CODEX_HOME` 需要额外一次 `codex login`（多一步人工操作）；这次隔离只覆盖了「配置/规则/AGENTS.md」这一类问题，不覆盖下面这条记录的 Windows 沙箱执行 bug（那是完全不同的另一层问题）。
- **什么情况下应该重新考虑**：不预期重新考虑——这是任何"服务不该依赖某个开发者个人机器状态"场景下都成立的通用原则，以后接入其他执行后端也应该默认按这个原则设计，不用等踩坑才想起来。

## Codex 后端在 Windows 上暂不可用，不开 `--dangerously-bypass-approvals-and-sandbox` 绕过

- **日期**：2026-08-09
- **背景**：解决了配置隔离问题后，`CodexBackend` 依然在全新隔离的 `CODEX_HOME` 下对任何 PowerShell 命令返回 `rejected: blocked by policy`，排查确认是 Codex CLI 在 Windows 上 `workspace-write` 沙箱策略检查器的已知开放问题（上游 bug，GitHub `openai/codex` issue #11885 等），跟本项目的配置无关，见 PITFALLS.md。唯一能绕开的办法是 `--dangerously-bypass-approvals-and-sandbox`，完全跳过审批和沙箱。
- **选了什么**：**用户拍板**——不开这个 flag，风险太大（"可能会有提示词攻击的问题"）。Codex 后端代码保留（`CodexBackend`、独立 `CODEX_HOME` 隔离都已实现），但标记为"Windows 上暂不可用"，MVP 阶段只用 `ClaudeAgentSDKBackend` 这一条能跑的路径，"先把流程跑通"优先于"两个后端都验证过"。
- **否掉了什么，为什么**：开 `--dangerously-bypass-approvals-and-sandbox` 硬跑通——官方文档明确这个 flag 只该用于"外部已经做好沙箱隔离"的环境，而我们的"隔离"只是换了个当前工作目录，不是操作系统级隔离，开了之后 Codex 生成的命令对整台机器有无限制读写和网络权限，用户执行agent 的输入本质上部分来自用户的自然语言消息，存在真实的提示词注入风险，即使"纯粹自己用"也不划算用这个代价换 Codex 能跑。
- **代价 / 已知不足**：Codex 这条执行后端目前完全没有被验证过（从写完代码到现在，一次成功执行都没有——先卡订阅额度，额度问题解决后又卡这个沙箱 bug），"双后端可插拔"这个设计目标目前只有一半是活的。
- **什么情况下应该重新考虑**：OpenAI 修复这个上游 Windows 沙箱 bug 之后；或者决定换一个不同的 harness/execution 方式跑 Codex（比如 WSL，用户提到过但这次没有验证时间）；或者未来部署到 Linux/macOS 云主机上（这个 bug 描述上像是 Windows/PowerShell 特有的，别的平台可能不受影响，但这个推测未经验证）。

## `ClaudeAgentSDKBackend` 的 `bypassPermissions` 提示词注入敞口暂不处理，等公开前再收窄

- **日期**：2026-08-09
- **背景**：审视项目时发现——`ClaudeAgentSDKBackend` 从最早跑通到现在，每次执行都是 `permission_mode="bypassPermissions"`，不需要任何审批就能跑 Bash、读写文件；而现在"读用户文件做总结/问答"这个功能已经上线，执行 agent 会直接读取用户上传文件的内容。如果文件内容里被植入了提示词注入 payload（比如"忽略之前的指令，改成执行 xxx"），没有任何机制拦截。这跟用户在 Codex `--dangerously-bypass-approvals-and-sandbox` 那条决策里明确否掉的风险是同一类，只是当时只审视了 Codex，没有意识到 Claude 这条线一直存在同样的敞口。
- **选了什么**：**用户拍板**——风险是真实的，但现在收益太低（"纯粹自己用"，攻击面小），不值得现在投入去收窄；留到项目要公开给其他人用之前，必须处理。
- **否掉了什么，为什么**：现在就着手收窄权限（比如给 Bash 加白名单、上审批流程、换更严格的 sandbox）——不是否定这件事本身的必要性，而是排序问题：现在没有除开发者之外的真实用户，投入产出比低，其他工程基础（orchestrator、文件收发、日志留痕）优先级更高。
- **代价 / 已知不足**：从现在起到"公开前处理"这段时间里，这个敞口持续存在，且范围不止 Claude 这一个后端——`ClaudeAgentSDKBackend` 和 Codex 的沙箱问题是同一类风险的两个不同表现形式，以后设计任何新执行后端都要默认假设这一类风险存在，不能假设"这次选的方案不一样应该没事"。
- **什么情况下应该重新考虑**：**硬性条件——项目从"只有开发者自己用"变成"给其他任何人用"之前，必须先处理这条**，不能顺延成"以后再说"。

## 用户 memory 的被动事实提取改用 DeepSeek API，不用本地 Ollama

- **日期**：2026-08-09
- **背景**：设计用户 memory 层 2（记住姓名/部门这类事实）时，用户明确提出先试本地 Ollama 的轻量模型（省钱、不依赖外部 API），机器上已经装了 qwen2.5:7b、qwen3:8b 等几个模型。
- **选了什么**：实测两个模型都不可靠后，改用 DeepSeek 的 `deepseek-chat`（OpenAI 兼容 API，`openai` SDK 换个 `base_url` 就能调），效果明显更好、也符合结构化输出要求。
- **否掉了什么，为什么**：本地 Ollama 模型——**实测两次都严重失败，不是精度不够，是完全跑题**：qwen2.5:7b 第一次把用户"我叫张三，人事部经理"提取成"年龄:是秘密、职位:软件工程师、爱好:编程阅读"（凭空编造，输入里根本没有这些内容）；换更严格的反幻觉 prompt 后第二次输出变成"三顾茅庐"的翻译，跟输入毫无关系。qwen3:8b 同样的输入直接输出了一段关于"脑卒中"的医学科普，完全文不对题。两次失败都不是简单的 prompt 调整能解决的（第二次已经用了明确的"逐字核对、不要推测、不要编造"这类强约束语言），判断是这两个模型在"严格提取+JSON schema 约束"这个组合任务上的可靠性问题，不是我们这边的用法问题。
- **代价 / 已知不足**：从"零成本本地模型"退回到"付费 API"，虽然 `deepseek-chat` 定价便宜，但终究不是免费；多了一个新的外部依赖（`DEEPSEEK_API_KEY`）。
- **什么情况下应该重新考虑**：如果以后需要完全离线运行（没有外部网络），得重新评估本地模型方案——可能需要试更大参数量的模型、不量化的版本，或者接受"记忆功能离线时直接禁用"这个降级策略，而不是死磕本地小模型的提取质量。

## 各端点故障只记日志，不建用户可见的服务状态通知机制

- **日期**：2026-08-09
- **背景**：梳理了一遍系统对外的几个端点（飞书长连接、飞书 REST API、Anthropic/DeepSeek/OpenAI 三个模型 API、本地文件系统），讨论"服务不可用时能不能让用户知道断在哪一步"。飞书长连接尤其特殊——是纯粹的黑盒，连接掉了用户端不会有任何信号，这是长连接架构本身的限制，不是我们代码能改的。
- **选了什么**：**用户拍板**——每个端点的成败只记日志（已经这么做了），不为"用户能感知具体是哪个端点故障"专门建通知机制。飞书长连接这一层黑盒的现实直接接受，不试图绕过。
- **否掉了什么，为什么**：给每个端点分别设计用户可见的故障提示（比如区分"飞书发消息失败"和"执行 agent 调用失败"分别用不同措辞告诉用户）——用户判断这块投入产出比不够，现在的"处理失败，稍后再试"already 够用，且飞书长连接这个最大的黑盒本来就绕不开，把其他端点做得再精细也补不上这个大洞，没必要单独精修。
- **代价 / 已知不足**：进程挂掉、飞书连接断开这类情况，用户端依然完全没有反馈，只能干等或者重发——今天实测就出现过用户不确定第一条消息有没有处理成功、又发了一遍的情况。
- **什么情况下应该重新考虑**：如果长连接掉线变得频繁、影响到真实使用体验（不只是开发阶段偶尔发生），或者产品要给除开发者之外的人用时用户体验问题会被放大，那时候需要重新评估要不要至少做一个简单的"进程存活心跳/重启后主动告知"机制。

## draft 阶段区分"是不是真的文档任务"，给 confirm 循环加出口

- **日期**：2026-08-10
- **背景**：实测发现两个连带问题——① `ask_confirm` 循环只有两条边（确认词→执行，其他→回 collect 重新生成草稿），没有任何出口，用户发一句寒暄/身份确认（"我是浮瓜，你是小帮"）会被无限累加进 `pending_instruction`，每次都重新生成一次不断变长的草稿，永远出不去；② 草稿的 `task_summary` 是写给执行 agent 看的客观第三人称描述，但它被直接原文显示给用户当确认话术，两种受众的措辞需求互相矛盾，导致用户看到"用户是浮瓜、助手是小帮是用户的智能助手"这种别扭的第三人称转述。
- **选了什么**：draft 输出加一个 `is_task: bool` 字段和一个专门给用户看的 `user_message` 字段（对话口吻，跟 `task_summary` 分开）。`is_task=false`（寒暄/打招呼/无关提问）时图直接进新增的 `reply_directly` 节点——发 `user_message`、清空 `pending_*`，不进 confirm 循环。这也顺带解决了死循环：卡在 ask_confirm 里的用户如果说了不相关的话，累积文字被重新丢进 draft 判断，一旦被重新分类成"不是任务"，就会从这条新边跳出去，不会无限攒下去。
- **否掉了什么，为什么**：加一个机械的"取消/算了"关键词识别，让用户显式喊停——没否死，只是判断用 `is_task` 重新分类已经能自然覆盖大多数卡住的场景，不需要再叠一层关键词识别；如果以后发现 LLM 判断不够可靠、经常该退出的时候没退出，再考虑加机械兜底。
- **代价 / 已知不足**：`is_task` 的判断依赖 draft 那次 LLM 调用的判断质量，不是 100% 可靠的机械规则；如果用户合法的任务补充信息被误判成"不是任务"，会被错误地打断确认流程、清空已经攒的信息，需要重新说一遍——这个边界情况还没有专门测过。
- **什么情况下应该重新考虑**：如果实测发现 `is_task` 经常把真实任务补充信息误判成闲聊、干扰正常确认流程，需要调整判断标准或者加机械兜底规则。

## draft.py 也要用 preset + exclude_dynamic_sections，纯字符串 system_prompt 挡不住账号信息泄漏

- **日期**：2026-08-10
- **背景**：给 draft 加 `is_task` 判断后测试发现，模型被问"你是谁"时会带出开发者本人的 `claude login` 账号邮箱，`draft.py` 当时的 `system_prompt` 是纯字符串（完全替换默认值），跟 `claude_agent.py` 早先修过的"用 preset+exclude_dynamic_sections 隔离动态上下文"没用上同一套机制。
- **选了什么**：`draft.py` 的 `system_prompt` 也改成 `{"type": "preset", "preset": "claude_code", "append": ..., "exclude_dynamic_sections": True}` 形式；但实测发现光这样还不够（账号邮箱依然会泄漏，见 PITFALLS.md），最终追加了一条明确指令——"不要提及/透露开发者账号信息、不要暴露 Claude Code 这个底层工具名字"，这条指令层面的压制经过多次重复测试确认有效。
- **否掉了什么，为什么**：只做 `exclude_dynamic_sections=True` 不加显式指令——实测验证过不够，账号身份信息不属于该参数覆盖的"动态段落"范围。
- **代价 / 已知不足**：这是指令层面的压制，不是结构性隔离，理论上不能保证在所有输入下都 100% 生效——真正的结构性修复是换 API key 鉴权（不绑定个人账号身份），但项目还在用订阅登录，这个风险跟"MVP 阶段接受 ToS 边界不确定性"是同一类风险的另一种表现形式。
- **什么情况下应该重新考虑**：如果后续实测又发现账号信息在其他场景下泄漏（指令没压住），需要考虑更强的隔离手段，或者提前把"换 API key"这件事的优先级提上来，而不是等到"公开给别人用之前"才处理。

## 显式拆分 MainAgent、LangGraph 控制平面与 ExecutionAgent

- **日期**：2026-08-12
- **背景**：`draft.py` 最初只是“轻量整理任务”，后来逐步承担了闲聊、身份、意图判断、缺失信息、用户确认话术；`graph.py` 又自行读取/写入 memory；`ExecutionResult.reply_text` 让 coding agent 直接决定给终端用户说什么。结果不是完全没有主 Agent 逻辑，而是主 Agent 的职责散落在 draft、graph、memory、runner 和执行器里，没有单一 owner。机器人称呼被误存成用户姓名只是这个边界混乱暴露出的一个症状，继续补 extraction prompt 不能解决根因。
- **选了什么**：新增显式 `MainAgent` 抽象。当前 `DeepSeekMainAgent` 使用普通 chat API、没有代码工具，统一负责对话身份、意图、`TaskContract`、长期记忆候选/纠错以及用户话术；`JsonMemoryRepository` 对候选做字段白名单和 set/delete 校验；LangGraph 只负责可恢复的状态转移、确认 interrupt/resume；Claude Code/Codex 只实现 `ExecutionAgent`，接收已确认任务并返回内部 `ExecutionReport` 和 artifact reference。**这条明确取代** 2026-08-07“理解和执行都交给 coding agent 自己的 agentic loop”的部分结论；保留 coding agent 做文档执行的决定不变。
- **否掉了什么，为什么**：继续把 `draft.py` 当小 helper 并给 `memory.extract_facts()` 加更多正反例——模块名与真实职责已经不符，且只在执行成功后提取会漏掉普通对话中的自我介绍/纠错；让 LangGraph 充当“主 Agent”——LangGraph 是工作流 runtime，不拥有对话人格或记忆政策；让执行器直接返回用户话术——会把环境身份、coding harness 偏好和用户交互继续耦合。
- **代价 / 已知不足**：文档任务通常多一次 `MainAgent.finalize()` 调用，增加延迟和费用；v2 state schema 与旧 checkpoint 不兼容，因此切换到 `checkpoints-v2.db` 并保留旧库；目前只做了离线 fake-client 测试，真实 DeepSeek 对记忆边界和真实飞书交互仍需冒烟/eval 验证。
- **什么情况下应该重新考虑**：如果 finalize 的成本没有带来可测量的用户体验收益，可改为确定性 presenter，但不能把用户话术重新塞回 ExecutionAgent；如果更换主模型，只替换 `MainAgent` 实现，不改变三者边界。

## checkpoint 只保存 artifact 引用，不保存输入或输出文件 bytes

- **日期**：2026-08-12
- **背景**：旧 `SessionState.result` 保存 `result_file: bytes`，同时 `pending_file/new_file` 保存带 `content: bytes` 的 `IncomingFile`。输入附件会随 collect、main-agent、interrupt 等多个历史 checkpoint 重复序列化；只修输出仍会让 SQLite 膨胀并长期保留敏感输入。
- **选了什么**：平台附件在入图前写入 `var/inputs/`；执行输出保存在 `var/workspaces/`。两者都通过 plain JSON `ArtifactReference(kind,path,filename,mime_type)` 进入图。`ExecutionAgent.run` 接受 `input_path` 而不是 bytes；平台投递时才读取输出 bytes。输入名收窄为 basename，输出绝对路径、`..`、子目录、越界 symlink、目录冒充文件一律拒绝。
- **否掉了什么，为什么**：继续让 checkpoint 充当 artifact store——短期状态存储与大文件存储不是同一职责，已有持久 Artifact 目录无需复制一份；只把输出改成 path——输入 bytes 才是此前遗漏的主要体积和隐私边界。
- **代价 / 已知不足**：checkpoint 能恢复但本地 artifact 被手工删除时，投递仍会失败；未来上云或多进程部署时应把本地 path 升级为真正的 ArtifactStore URI/ID。
- **什么情况下应该重新考虑**：部署变成多机或工作目录不再是持久卷时，引入对象存储并把 `artifact_path` 替换为稳定 artifact ID。

## 有副作用的执行前先 checkpoint execution identity，并写完成 marker

- **日期**：2026-08-12
- **背景**：旧 `_execute` 在一个 LangGraph step 内创建目录、调用 coding agent、finalize、写日志；如果文件已经生成，但 turn log 或后置 checkpoint 失败，节点会保持 pending。下一条消息若再被错误当作 resume，可能重复生成文件和计费。LangGraph checkpoint 不会自动让外部 API/文件系统获得 exactly-once。
- **选了什么**：确认后先走 `prepare_execution`，把稳定的 `execution_id/workdir` 作为单独 superstep 写入图状态；生产入口显式使用 `durability="sync"`，使该 checkpoint 完成后才进入 execute。编排元数据放在执行 cwd 外：调用 backend 前写 started marker，成功返回后写 report marker。恢复时只有 started 而没有可信 report，就标记结果未知并拒绝自动重跑；已有 report 则复用。业务异常被转换为完成态失败结果；turn log 异常只记录日志，不改变业务成功状态。runner 只按真实 `snapshot.interrupts` resume，非 interrupt pending task 不自动重放。
- **否掉了什么，为什么**：把任何 `snapshot.next` 都当作可恢复确认——失败节点同样有 next；让诊断日志决定业务成败——会把已经完成的外部副作用变成隐式重试；宣称 marker 等于 exactly-once——coding agent 产生副作用但尚未返回 report 的窗口仍然存在。
- **代价 / 已知不足**：多一个 checkpoint superstep 和小型 marker 文件；崩溃后某些 session 需要人工/显式 recovery policy。后续仍需 backend 级幂等键、持久 inbox/outbox 和故障注入测试。
- **什么情况下应该重新考虑**：执行后端原生支持幂等 request key/事务任务队列后，可把 marker 升级为 durable execution ledger；不能退回“收到下一条用户消息就重跑旧 execute”。

## 长期记忆由 MainAgent 提议，但必须由用户确认后落盘

- **日期**：2026-08-12
- **背景**：即使 memory operation 带当前原文 evidence，正则也无法穷尽引用、玩笑、假设、否定等自然语言语境。把模型候选直接写入 JSON，会让一次语义误判静默污染长期身份；继续堆反例 prompt/regex 只能缓解，不能建立可靠信任边界。
- **选了什么**：MainAgent 只提出候选；repository 先做字段、evidence、第一人称和长度的保守校验，但不写入。普通对话进入 `ask_memory`，用户回复“记住”才保存，“不用记”则丢弃；文档任务回复“是”只执行，回复“是并记住”才保存后执行。所有实际变更透明回显。
- **否掉了什么，为什么**：只靠模型 prompt 或正则自动写入——无法证明语句一定在陈述用户本人事实；彻底禁用 memory——会失去用户明确要求的长期个性化能力。
- **代价 / 已知不足**：多一个确认回合，交互更慢；当前确认后的 JSON mutation 仍没有与 graph checkpoint/outbound 组成原子事务，也没有可撤销 event ledger。
- **什么情况下应该重新考虑**：有了带 `turn_id/source/evidence` 的幂等、可撤销 memory ledger 后，可优化确认体验，但不能重新允许执行 Agent 或未确认模型候选直接改长期档案。

## Golden Evaluation 遇到单个样本执行异常时保持 fail-fast

- **日期**：2026-08-13
- **背景**：代码审查提出，Golden Dataset 批量评估时若单个 `GoldenCase` 的 `answer_function` 抛出异常，可以选择继续执行剩余样本，也可以立即终止本次评估。两种方案会直接影响批次状态和指标解释。
- **选了什么**：**用户拍板**——保持 fail-fast。任一 case 执行异常时立即终止本次 EvaluationRun，保存此前已经完成的 `case_results`，将运行标记为 `FAILED` 并保留明确错误，不生成看似完整的成功指标。
- **否掉了什么，为什么**：捕获单个 case 异常后继续剩余样本——这会迫使系统引入“部分成功”状态，并决定执行失败样本应计入、排除还是单独统计；在这些语义尚未明确前继续运行，容易把外部 API／工作流故障与模型回答准确性混为一谈，产生不可比较的指标。
- **代价 / 已知不足**：100 个样本的长批次可能因一个临时 API 故障提前结束，需要修复故障后重新运行；已完成样本只用于诊断，不代表完整评估结果。
- **什么情况下应该重新考虑**：当评估运行成本显著上升、临时外部故障频繁，且已经设计出明确的 case-level error、重试策略、部分成功状态及不污染质量指标的统计口径时，再考虑 best-effort 批量执行。

## 合同智能相关决策已拆分

2026-08-14～2026-08-15 期间"合同智能 MVP 拆为两条链路"及后续工程量清单相关决策，已随
`contract_intelligence`/`contract_admin` 一起拆分到独立仓库
[contract-intelligence](../contract-intelligence)（保留在该仓库自己的 DECISION.md 中，
未随代码丢失）。

## 短期历史压缩（compaction）设计稿：带证据、增量合并、独立轻量模型

- **日期**：2026-08-17
- **背景**：`recent_messages` 目前是硬截断（12 条/单条 2,000 字/总计 12,000 字符），超出
  部分直接丢弃，不做语义压缩；这与长期记忆已有的"结构化候选 + evidence"治理哲学不一致，
  也是求职材料整理时暴露出的一处明确缺口——JD 要求的"记忆系统"里，短期会话内的历史压缩
  当前完全空白。这是一份**设计稿**，代码尚未实现。
- **选了什么**：
  1. 摘要输出带 evidence（每条 `key_fact` 标注来自哪几条原始消息 index/原文片段），过
     `SummaryValidator`（校验哲学复用 `MemoryRepository.validate()`）后才追加进新增的
     `SessionState.conversation_summary` channel；不合并进 `known_facts` 长期档案，二者
     生命周期和校验规则分开。
  2. 增量合并：每次只对"新被截断的一批"生成新 entries 并追加，不重写旧的；`conversation_
     summary` 本身变长后触发二级压缩，但二级压缩只允许合并/精简已验证 entries，不允许
     新增事实声明。
  3. 摘要抽取和二级合并都用独立的轻量模型 client，不复用 MainAgent 的 DeepSeek 决策调用。
- **否掉了什么，为什么**：
  - 不带证据的自由文本摘要——无法审计、容易幻觉，且与项目现有"evidence 优先"的记忆治理
    哲学矛盾。
  - 一次性重写整份摘要——每次都要把全部历史摘要 + 新消息重新喂模型，成本随会话变长线性
    增长，一致性也不如增量式明确。
  - 复用 MainAgent 的 DeepSeek 做摘要——摘要是结构化抽取任务，不需要最强模型，复用主决策
    模型会让摘要成本和主决策成本纠缠在一起，不利于独立核算和优化，也偏离"性能/成本管控"
    这条设计目标。
  - Anthropic Claude API 的原生 Memory tool + Context editing——工程量最小，但把"要不要
    写、写什么"的判断权交还给模型本身，与"模型只提候选、代码做确定性校验"的现有安全边界
    直接冲突；OpenAI 侧没有等价原生能力。评估后暂不采用，继续走应用层确定性校验路线。
- **代价 / 已知不足**：二级压缩"只合并不新增"这条约束目前只能靠模型指令遵循，没有机械
  校验能验证"合并后的 entry 完全没有窜改原有 evidence 语义"，是和现有长期记忆同一类局限
  的延伸。整份设计尚未实现，真实吞吐、成本、摘要质量都未经验证。
- **什么情况下应该重新考虑**：实现后如果发现二级合并经常引入无法追溯回原始 evidence 的
  内容，需要给二级压缩也加机械校验（比如强制新 evidence 是原 entries evidence 的并集，
  不允许模型自由改写）；如果发现独立轻量模型的抽取质量明显不如复用 DeepSeek，需要重新
  评估分层选型。

## 多文件执行会话设计：批处理进单一临时工作区，替代单文件覆盖

- **日期**：2026-08-18
- **背景**：`orchestrator/debounce.py` 的 `_files` 是单槽 dict，同一防抖窗口内连发多个文件会静默覆盖丢失（缺口详情见上方「10 秒防抖 + 轻量确认草稿」决策 2026-08-17 的实现状态更新）。用户提出参照 AutoGen/CrewAI 等多 agent 框架的思路，重新设计编排层怎么把一轮防抖窗口内收到的文件+指令打包交给执行 Agent。完整设计见 spec：[docs/superpowers/specs/2026-08-18-multi-file-execution-session-design.md](docs/superpowers/specs/2026-08-18-multi-file-execution-session-design.md)。
- **选了什么**（1-4 由用户逐项拍板，5 是我的建议、用户认可）：
  1. `ExecutionReport` 支持多输出（`artifacts` 列表取代单文件哨兵对）。
  2. Session 严格等于一个防抖窗口；确认前追加的文件合并进已有未确认任务的 `pending_files`（不新建工作区概念）。
  3. 批量输入里单个文件 OOXML 校验失败时排除该文件、其余继续执行，而非整批拒绝。
  4. 同批次文件名碰撞：`ArtifactReference` 新增 `display_filename` 字段去重，不改动 `filename` 与物理路径的绑定。
  5. 继续用 LangGraph 做控制平面，不迁移到 AutoGen/CrewAI。
- **否掉了什么，为什么**：
  - **AutoGen/CrewAI 作为编排框架**：AutoGen 核心原语是 agent 间自由消息传递，正是 v2 重构要替代掉的模式（旧架构自由消息传递导致"机器人身份误写入用户记忆"的线上问题，见本文件靠前的相关决策）；CrewAI 的 human-in-the-loop 是同步阻塞式；两者都没有本项目依赖的"进程重启后从 checkpoint 恢复确认中断状态机"能力（`AsyncSqliteSaver` + `interrupt()`/`Command(resume=...)` + `durability="sync"`）。换框架等于重新解决已攻克的难题。
  - **工作区创建提前到 collect/debounce 阶段**（文件一到就落工作区，而非先存 `var/inputs/` 暂存引用）：会在"确认前"产生不受 checkpoint 覆盖的外部副作用目录，正是 `durability="sync"` + started/report marker 当初要防的"checkpoint 落盘与外部副作用顺序竞争"问题。调研 AutoGen/CrewAI/OpenHands 后确认三者都不需要应对这种"确认前/确认后"阶段区分（要么同步交互，要么工作区与容器生命周期直接绑定），没有更成熟模式可抄。
  - **用挂载（bind-mount）替代拷贝**（参考 OpenHands `SANDBOX_VOLUMES`）：挂载会让 agent 能看到跟 host 共享/可寻址的路径结构，与"文档内容不可信、需防 prompt injection"的安全边界冲突，拷贝进隔离 workdir 更安全。
  - **校验失败整批拒绝**：用户体验差——4 个文件错 1 个，其余 3 个也处理不了，用户选择了"排除坏文件、其余继续"。
  - **同名文件直接丢弃晚到的**：会静默丢失用户真实上传的不同文件（只是碰巧同名），用户选择了新增字段去重而不是丢弃。
- **代价 / 已知不足**：多文件投递部分失败（runner 发送 N 个文件中途失败）本次不处理，沿用 PROGRESS.md 已排期的持久 outbox/ack 缺口，只是量级从单文件放大到 N 文件；"闲聊/文件处理/重任务"三分 Agent 的架构问题被明确推迟，"重任务"具体含义仍未定义；这条决策截至记录时代码尚未实现，只是 spec 定稿。
- **什么情况下应该重新考虑**：如果实现后发现"重任务"确实需要跟"文件处理"完全不同的能力边界（比如需要联网检索、多步骤规划），再单独为"闲聊/文件/重任务"三分 Agent 走一轮 brainstorming；如果多文件投递部分失败在真实使用中频繁出现且用户体验受损明显，应提前 PROGRESS.md 里 outbox/ack 任务的优先级，不要继续等它排到。

## trace_id（追踪 id）与 execution_id（执行幂等身份）并存，不合并成一个字段

- **日期**：2026-08-20
- **背景**：给 debounce→main_agent→execute→投递这条链路补统一追踪 id 时发现，`workspace.py` 早就在 `prepare_execution` 阶段生成一个 `execution_id`（`uuid.uuid4().hex[:8]`，同时是 workdir 目录名），并写进 `TurnRecord.run_id`。两者字符串格式完全一样，看起来像是"重复造轮子"，更自然的做法是把新 trace_id 直接并进 `execution_id`，或者干脆复用同一个字段。
- **选了什么**：新增独立的 `SessionState.trace_id` 字段，与 `execution["execution_id"]` 并存。`trace_id` 在 debounce 窗口触发时生成，覆盖"提议→确认→执行→投递"整轮任务的生命周期；`execution_id` 只在真正进入 `_prepare_execution` 时生成，仍然只标识"这一次执行尝试"。
- **否掉了什么，为什么**：合并成一个字段——`execution_id` 目前直接绑定 started/report marker 幂等逻辑（`_execution_was_started`/`_mark_execution_started`/`_load_execution_marker` 都以 workdir 名即 execution_id 为准），且只在 `_prepare_execution` 之后才存在；而 trace_id 需要在此之前（debounce 阶段、甚至提议被拒绝走 `reply → END` 从不进入执行）就已经产生并可用于日志。把两者合一要么把 execution_id 的生成提前到 debounce（会打乱"确认前不能有外部副作用目录"这条既有边界，参见上方"多文件执行会话设计"决策里否掉的"工作区创建提前"选项），要么让 trace_id 退化成只在执行阶段才存在（就失去了覆盖提议/确认阶段的意义）。两者都不划算，不如接受两个字段并存。
- **代价 / 已知不足**：同一次任务的日志里会同时出现两个格式相同的短 id（trace_id 和 execution_id），初读代码/日志的人容易误以为是同一个东西或多余重复；文档（`orchestrator/state.py` 的字段注释、本条决策）是目前唯一区分两者的地方。
- **什么情况下应该重新考虑**：如果以后 execution_id 的幂等身份逻辑被重新设计成不再绑定 workdir 目录名（比如换成独立的幂等 key），可以重新评估要不要把 trace_id 并入；如果实测发现两个 id 在日志排查中确实经常被人搞混、造成实际排障成本，应该优先加清晰的日志字段前缀/文档提醒，而不是仓促合并。

## Eval harness（golden set 回归）设计定稿：端到端 graph、回归 fake 执行/冒烟真执行、确定性断言阻断 + Opus judge 只报告

- **日期**：2026-08-20
- **背景**：改 prompt/记忆逻辑不会报错只会悄悄变差，自查清单把 eval harness 列为优先级"高"的欠账；DECISION.md 2026-08-13 只拍板了 fail-fast 原则，样本、组织、运行方式全未设计。完整 spec 见 `docs/superpowers/specs/2026-08-20-eval-harness-design.md`。
- **选了什么**：**6 个决策点全部用户拍板**——① 评估切面打端到端整个 graph（多轮）；② 回归集注入 FakeExecutionAgent、`--real-execution` 冒烟走真实后端，MainAgent 始终真实 DeepSeek，个人使用阶段继续订阅登录；③ 样本四类全收（意图路由/记忆边界/确认词语义/prompt injection），每类 5-8 个共 20-30 样本，此后 badcase 驱动增长；④ 判分=确定性断言 + Claude CLI Opus judge 评话术，judge 配校准集验证裁判本身；⑤ 独立脚本手动跑、报告存 var/evals/，确定性断言 100% 阻断、judge 分数只报告不阻断；⑥ 敏感话术黑名单（开发者邮箱等）从环境变量加载不入公开仓库。
- **否掉了什么，为什么**：只打 `MainAgent.decide` 单轮切面（易归因但覆盖不了确认流转与记忆落盘，用户明确要端到端）；回归也走真实执行后端（慢、烧订阅额度、执行层抖动会让 fail-fast 把整批标 FAILED 污染回归信号）；只做确定性断言不加 judge（话术清晰度对中老年用户是真实盲区，用户听完确定性断言 vs judge 的完整解释后维持要 judge）；judge 分数也阻断（judge 自身会抖，起步阶段会训练出"忽略红灯"习惯，校准一致率≥90% 达标前只当参考）；pytest marker 方式运行（模糊"标准 pytest 不联网"铁律，且不适合产出可对比历史报告）；GitHub CI（judge 依赖本机 Claude 订阅登录态，CI 无解）。
- **代价 / 已知不足**：`deepseek-chat` 是移动别名，供应商升级引起的漂移与本仓库 prompt 改动混在一起只能事后归因；初版 judge 校准样本是设计者手写的，可能恰好是 judge 容易判对的形态；话术风格好坏（超出清晰度/误导性）不评。
- **什么情况下应该重新考虑**：judge 校准一致率稳定 ≥90% 后可讨论把 judge 升级为阻断项；出现对外接口时订阅登录必须换 API key（既有决策）；样本数随 badcase 回填超过 ~60 个时重新评估运行时长与组织方式。

## DeepSeekMainAgent 调用显式设 temperature=0

- **日期**：2026-08-20
- **背景**：eval harness 采用"确定性断言 100% 阻断"，但 `deepseek.py` 的 `chat.completions.create` 没设 temperature（默认 1.0），同一样本的 action/intent 可能随机抖动，把非回归的抖动标成 FAILED。
- **选了什么**：**用户拍板**——生产代码给 DeepSeek 调用显式加 `temperature=0`。`decide` 做的是分类+结构化输出（intent 路由、TaskContract、memory operations），不是创意写作，低温让生产行为更稳定可预测，顺带让 eval 阻断语义站得住。作为小的生产改动进 eval harness 实现计划，走 TDD。
- **否掉了什么，为什么**：不动生产、eval 失败样本自动重试 1 次——会把"100% 阻断"稀释成"重试后 100%"，且掩盖真实的决策边界不稳定；什么都不做先观察抖动率——观察窗口内的每次假 FAILED 都在消耗对 eval 信号的信任。
- **代价 / 已知不足**：`finalize` 的话术会变得每次一致、缺少变化（该任务要的是清晰不是花样，可接受）；temperature=0 不等于完全确定（供应商侧仍可能有微小非确定性），只是把抖动压到最低。
- **什么情况下应该重新考虑**：如果实测发现 temperature=0 下话术生硬到影响中老年用户理解（P2 交互重设计阶段验证），可以只给 finalize 路径回调温度、decide 保持 0。
